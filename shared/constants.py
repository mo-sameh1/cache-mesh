PROJECT_VERSION = "0.1.0"

SERVICE_NAMES = {
    "gateway": "gateway",
    "name_service": "name-service",
    "replica": "replica",
    "inference_adapter": "inference-adapter",
}

FAULT_MODES = {
    "disabled",
    "read_timeout_once",
    "pause_node",
}

